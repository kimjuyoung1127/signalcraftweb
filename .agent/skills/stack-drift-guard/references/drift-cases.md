# Drift Cases

## Stable

- stack, boundaries, verify commands, naming contract가 profile과 대부분 일치

## Drifting

- 새 worker나 integration이 생겼지만 profile은 아직 버틸 수 있음

## Reprofile Needed

- stack line은 frontend-only인데 실제로 backend, worker, new contract layer가 커졌음
- verify commands와 boundaries가 현재 profile을 더는 설명하지 못함
